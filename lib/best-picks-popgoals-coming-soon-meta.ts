/**
 * Visitor-facing metadata for the Best Picks PopGoals “coming soon” strip.
 * Icon: `public/images/popgoals-icon.png` (same as `apps-data` PopGoals entry).
 */
export const bestPicksPopgoalsComingSoonMeta = {
  displayName: 'PopGoals',
  /** Public asset (see `apps-data` PopGoals entry). */
  iconSrc: '/images/popgoals-icon.png',
} as const;
